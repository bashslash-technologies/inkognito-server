'use strict'

const verificationTemplate = `Use this code to verify your Inkognito account,\n code: CODE \n expiry: EXPIRY`

const resetTemplate = `Use this code to verify your Inkognito account,\n code: CODE \n expiry: EXPIRY`

module.exports = {
	verificationTemplate: verificationTemplate,
	resetTemplate: resetTemplate
}